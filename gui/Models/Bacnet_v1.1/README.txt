The last layer trained using the basic retrain.py from the tensorflow website.
The graph is for the last layer of inception - trained on a limited set of data
(around 50 images per class). The labels are the names of a set of bacteria
(not the final type). Very first network!